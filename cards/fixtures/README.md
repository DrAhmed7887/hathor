# Synthetic vaccination card fixtures

This directory contains redistributable synthetic vaccination-card fixtures.
They use recreated mock layouts, fictional child identifiers, and invented
dates. They must not include real child records or ground truth copied from
real patient cards.

Use `synthetic_vaccination_cards/manifest.json` as the source of truth for
expected rows, digit systems, and negative controls.

